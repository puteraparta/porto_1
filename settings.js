// Where to load data from. Choose either a local file in GitHub repo, such as `data/places.csv`,
// or a Google Sheets URL (File > Publish to the Web > CSV )
const dataLocation = 'data/Format_Web_SLJIV.csv';

// Map's initial center, and zoom level
const mapCenter = [2.1278, 116.7377];
const mapZoom = 14;

// Marker icon height and width
const iconHeight = 6;
const iconWidth = 6;

//Icon for pohon muda and pohon dewasa
const sudahTebang =  'logo/pohon_dewasa.svg'
const belumTebang = 'logo/tree-5202.svg'
